version: '3'

services:
  java:
    container_name: mediaconverter_java
    build:
      context: .
      dockerfile: Dockerfile
#    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - .:/app:delegated
    tty: true
